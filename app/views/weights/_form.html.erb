<%= form_with model: @weight do |f| %>
  <%= f.label :体重 %>
  <%= f.number_field :value, step: '0.1' %>

  <%= f.label :日付 %>
  <%= f.date_field :input_time %>
  <%= f.submit '送信', class: 'btn btn-primary' %>
<% end %>